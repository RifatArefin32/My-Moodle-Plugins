<?php 
/**
 * Version details.
 *
 * @package   local_curriculum
 * @copyright 2024 Rifat Arefin
 * @license   http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */


 function allCourses(){
    // global $DB, $OUTPUT;
    // $categories = $DB->get_records('course_categories');
 
    // $categories_template = (object)[
    //     'category' => array_values($categories),
    //     'myurl' => new moodle_url('/local/curriculum/curriculum_list.php')
    // ];
    
    // echo $OUTPUT->render_from_template('local_curriculum/manage', $categories_template);
    // echo $OUTPUT->footer();
 }

 function local_curriculum_getCourses(int $id = null){
    // global $DB;

    // $actionurl = new moodle_url('/local/footballscore/edit.php');

    // if ($id) {
    //     $score = $DB->get_record('local_footballscore', array('id' => $id));
    //     $mform = new edit_form($actionurl, $score);
    // } else {
    //     $mform = new edit_form($actionurl);
    // }
    // return $mform;
}